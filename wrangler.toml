name = "current-moon-phase-emoji"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Cloudflare Pages will use this when you run `npx wrangler pages deploy`.
# For dashboard-based deploys via "Connect to Git", this file is informational
# — the dashboard fields override these values.
